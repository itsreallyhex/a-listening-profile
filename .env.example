# Path B only. Copy this to .env for local testing of the proxy with
# `vercel dev` or `netlify dev`. In production, set these in the host's
# dashboard instead, not in a file.

LASTFM_API_KEY=your_lastfm_api_key
LASTFM_USER=your_lastfm_username
